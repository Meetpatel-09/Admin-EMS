<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.adminems">

    <application
        android:allowBackup="true"
        android:icon="@drawable/c"
        android:label="@string/app_name"
        android:roundIcon="@drawable/c"
        android:supportsRtl="true"
        android:theme="@style/Theme.AdminEMS">
        <activity android:name=".LoginActivity" android:theme="@style/Theme.MaterialComponents.Light.NoActionBar">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".notice.DeleteNoticeActivity" />
        <activity android:name=".faculty.UpdateFaculty" />
        <activity android:name=".faculty.AddFaculty" />
        <activity android:name=".faculty.ViewFaculty" />
        <activity android:name=".UploadPdf" />
        <activity android:name=".UploadImage" />
        <activity android:name=".notice.UploadNotice" />
        <activity android:name=".MainActivity">

        </activity>
    </application>

</manifest>